services:
  - type: web
    name: spring-boot-ai-toolkit
    env: java
    buildCommand: ./mvnw clean install
    startCommand: java -jar target/ai-project-1.0.0.jar
    envVars:
      - key: JAVA_OPTS
        value: -Xmx512m
    plan: free
